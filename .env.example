# Copy this file to .env and fill in your own values. .env is gitignored.

# Free API key from https://console.groq.com -- only needed if you use the
# "Groq API (cloud)" generation option in the app. Local Ollama mode needs no key.
GROQ_API_KEY=
